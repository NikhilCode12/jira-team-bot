# Jira Cloud (only project + credentials in env; other fields from form)
JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_EMAIL=your-email@example.com
JIRA_API_TOKEN=your-jira-api-token
JIRA_PROJECT=ZRA
# Optional: Epic Link field ID (e.g. customfield_10014) to link issues to Epic ZRA-51. Leave empty to skip.
JIRA_EPIC_FIELD_ID=

# Chat/Teams: default component when not given in message
DEFAULT_CHAT_COMPONENT_NAME=RA_FE
# Optional: Jira component ID if name lookup fails
DEFAULT_CHAT_COMPONENT_ID=

# Free LLM (Groq - get key at https://console.groq.com)
GROQ_API_KEY=your-groq-api-key
GROQ_MODEL=llama-3.1-8b-instant
